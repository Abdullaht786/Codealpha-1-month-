# -*- coding: utf-8 -*-
"""Iris Dataset Exploration and Classification

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/#fileId=https%3A//storage.googleapis.com/kaggle-colab-exported-notebooks/iris-dataset-exploration-and-classification-1f65899d-0fd4-478c-a356-a5a6c5f0cc93.ipynb%3FX-Goog-Algorithm%3DGOOG4-RSA-SHA256%26X-Goog-Credential%3Dgcp-kaggle-com%2540kaggle-161607.iam.gserviceaccount.com/20240929/auto/storage/goog4_request%26X-Goog-Date%3D20240929T104938Z%26X-Goog-Expires%3D259200%26X-Goog-SignedHeaders%3Dhost%26X-Goog-Signature%
"""



"""The Iris dataset is one of the most famous datasets in the world of machine learning and statistics. It was introduced by the British biologist and statistician Ronald Fisher in 1936. This dataset is often used as a beginner's dataset for machine learning because of its simplicity and the fact that it contains only three classes of flowers: Iris-setosa, Iris-versicolor, and Iris-virginica.

In this notebook, we will explore the Iris dataset, visualize the data, and build a simple predictor to classify the species of the Iris flowers based on their features. If you find this notebook useful, please consider upvoting it.
"""

# Suppress warnings
import warnings
warnings.filterwarnings('ignore')

# Import necessary libraries
import pandas as pd
import numpy as np
import seaborn as sns
import matplotlib.pyplot as plt
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.neighbors import KNeighborsClassifier
from sklearn.metrics import accuracy_score, confusion_matrix, classification_report

# Load the dataset
df = pd.read_csv('/kaggle/input/iriscsv/Iris.csv')
df.head()

"""## Data Exploration"""

# Basic information about the dataset
df.info()

# Summary statistics of the dataset
df.describe()

"""## Data Visualization"""

# Pairplot to visualize the relationships between features
sns.pairplot(df.drop('Id', axis=1), hue='Species')
plt.show()

# Correlation heatmap
numeric_df = df.select_dtypes(include=[np.number])
correlation_matrix = numeric_df.corr()
sns.heatmap(correlation_matrix, annot=True, cmap='coolwarm')
plt.show()

"""## Building a Predictor"""

# Splitting the data into training and testing sets
X = df.drop(['Id', 'Species'], axis=1)
y = df['Species']
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.3, random_state=42)

# Standardizing the features
scaler = StandardScaler()
X_train = scaler.fit_transform(X_train)
X_test = scaler.transform(X_test)

# Training a K-Nearest Neighbors classifier
knn = KNeighborsClassifier(n_neighbors=5)
knn.fit(X_train, y_train)
y_pred = knn.predict(X_test)

# Evaluating the model
accuracy = accuracy_score(y_test, y_pred)
conf_matrix = confusion_matrix(y_test, y_pred)
class_report = classification_report(y_test, y_pred)
accuracy, conf_matrix, class_report

"""## Conclusion and Future Work

In this notebook, we explored the Iris dataset, visualized the relationships between features, and built a simple K-Nearest Neighbors classifier to predict the species of Iris flowers. The model's performance can be further improved by experimenting with different algorithms, tuning hyperparameters, or using more advanced techniques such as ensemble methods.

What other analyses or visualizations do you think would be useful for this dataset? Feel free to share your thoughts.

## Credits
This notebook was created with the help of [Devra AI data science assistant](https://devra.ai/ref/kaggle)
"""